# Memory Optimization Environment Variables
# Copy this file to .env.local and uncomment the options you need for RAM relief

# Node.js memory limit (adjust based on your system)
# NODE_OPTIONS="--max-old-space-size=2048"

# Jest worker configuration (1 = single worker, saves significant RAM)
# JEST_MAX_WORKERS=1

# Turbo build concurrency (1 = sequential builds, slower but memory-safe)
# TURBO_WORKERS=1

# Skip heavy browsers in E2E tests
# E2E_SKIP_BROWSERS="firefox,webkit,Mobile Chrome,Mobile Safari,Tablet"

# Skip client app in E2E tests (server-only testing)
# E2E_SKIP_CLIENT=1

# Disable memory-intensive features
# AI_ENABLE_AUTO_PROVIDERS=false
# RATE_LIMIT_ENABLED=false
# TELEMETRY_DISABLED=true
# NEXT_PUBLIC_FEATURE_AI=false
