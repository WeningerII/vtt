{
  "name": "@vtt/bots",
  "version": "0.0.0",
  "private": true,
  "description": "Simulated clients for load testing the VTT.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@vtt/logging": "workspace:*",
    "@vtt/net": "workspace:*",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "@types/ws": "^8.18.1"
  }
}
