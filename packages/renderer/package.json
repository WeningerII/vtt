{
  "name": "@vtt/renderer",
  "version": "0.0.0",
  "private": true,
  "description": "Instanced WebGL rendering engine for the VTT.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {},
  "devDependencies": {}
}