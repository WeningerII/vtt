{
  "name": "@vtt/monitoring",
  "version": "1.0.0",
  "description": "Production monitoring, logging, and alerting system for VTT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest",
    "lint": "eslint src/**/*.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@types/node": "^20.0.0",
    "prom-client": "^15.1.0",
    "winston": "^3.11.0",
    "nodemailer": "^6.9.7",
    "@types/nodemailer": "^6.4.14"
  },
  "devDependencies": {
    "@vtt/testing": "workspace:*",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  },
  "peerDependencies": {
    "@vtt/core-ecs": "workspace:*"
  }
}
