apiVersion: v1
kind: Service
metadata:
  name: client
  namespace: vtt
  labels:
    app.kubernetes.io/name: client
    app.kubernetes.io/part-of: vtt
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: client
  ports:
    - name: http
      port: 80
      targetPort: 8080
